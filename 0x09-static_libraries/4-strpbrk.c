#include "main.h"

/**
 * *_strpbrk - searchs a string
 * @s: string to search
 * @accept: dtring butes to ook
 *
 * Return: pointer to byte of string
 */
char *_strpbrk(char *s, char *accept)
{
	_putchar(*s);
	_putchar(*accept);

	return (s);
}
