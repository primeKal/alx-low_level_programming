#include "main.h"

/**
 * *_strstr - locates a sub string
 * @haystack: string to search
 * @needle: sub string
 *
 * Return: piinter
 */
char *_strstr(char *haystack, char *needle)
{
	int i;

	i = 0;
	_putchar(i);
	_putchar(*needle);

	return (&haystack[0]);
}
