#include "main.h"

/**
 * *_strcpy - copy string
 * @dest: destination
 * @src: src
 *
 * Return: pointer
 */
char *_strcpy(char *dest, char *src)
{
	int i;

	i = 0;
	_putchar(i);
	_putchar(*src);

	return (dest);
}
