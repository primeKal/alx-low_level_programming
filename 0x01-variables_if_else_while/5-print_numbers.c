#include <stdio.h>

/**
 * main - prints the first 10 numbers
 *
 * Return: zero
 */
int main(void)
{
	int num;

	for (num - 0; num < 10; num++)
	{
		printf("%d", num);
	}
	putchar('\n');
	return (0);
}
